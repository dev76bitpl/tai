# Tasks

Ten plik jest logiem wykonanych prac i backlogiem usprawnień.
Checklisty implementacyjne per faza znajdują się wyłącznie w [docs/ROADMAP.md](ROADMAP.md).

---

## Aktualny fokus

- [ ] Uzupełnij tę sekcję — co jest teraz w toku

---

## Backlog

- [ ] ...

---

## Stan sesji

- 2026-05-27: dodany `docs/CLAUDE_AUTH.md` — instrukcja per-projekt klucza API w `.claude/settings.local.json` + troubleshooting (m.in. `Auth conflict` warning powodowany przez pozostałości `oauthAccount`/`primaryApiKey` w `~/.claude.json` po `/logout`). Krok 8c dodany do `SETUP.md`, `_env_note` do `settings.local.json.example`.
- YYYY-MM-DD: ...
