{
  "_readme": [
    "Claude Code hooks — AI-specific layer.",
    "Git workflow guards (lint, tests, ADR, user-tested, etc.) live in .pre-commit-config.yaml at repo root.",
    "These hooks cover what pre-commit cannot: session context + AI template repo sync.",
    "Hook commands are in settings.local.json (gitignored, per-machine) — see settings.local.json.example."
  ]
}
